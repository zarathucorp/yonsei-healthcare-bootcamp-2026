---
title: "RCT 재현 프로젝트"
subtitle: "Synthetic data로 논문 분석 흐름 재현하기"
author: "김진섭"
date: "2026.07.21"
format:
  revealjs:
    theme: ../slides_assets/zarathu_theme.scss
    css:
      - ../slides_assets/custom-style.css
      - ../slides_assets/custom-style-fullbg.css
      - ../slides_assets/custom-xaringan-compat.css
    title-slide-attributes:
      data-background-image: ../slides_assets/bg.png
      data-background-size: cover
      data-background-opacity: "0.3"
    footer: "Zarathu Co., Ltd."
    self-contained: false
    include-after-body: ../slides_assets/header-inject-fullbg.html
    chalkboard:
      buttons: false
    preview-links: true
    show-notes: false
    slide-number: c/t
    width: 1600
    height: 900
execute:
  echo: true
  warning: false
  message: false
  eval: false
editor: visual
---

## 프로젝트 목표 {.intro-slide}

::: {.large}
마지막 실습에서는 논문 한 편을 흉내 내는 작은 프로젝트를 진행합니다. 실제 환자 데이터는 쓰지 않고, 재현 가능한 synthetic data로 RCT 분석 흐름을 익힙니다.
:::

::: {.highlight-box-blue}
- RCT 형태의 데이터를 만듭니다.
- baseline balance를 확인합니다.
- primary outcome을 분석합니다.
- survival endpoint를 분석합니다.
- 결과표와 그림을 발표자료 구조로 정리합니다.
:::

## Synthetic RCT 데이터 생성

```{r}
set.seed(2026)
n <- 600
rct <- data.frame(
  id = 1:n,
  trt = factor(sample(c("Control", "Treatment"), n, replace = TRUE)),
  age = round(rnorm(n, 64, 9)),
  sex = factor(sample(c("Female", "Male"), n, replace = TRUE)),
  diabetes = rbinom(n, 1, 0.28)
)

rct$risk <- -3.0 + 0.45 * (rct$trt == "Control") + 0.03 * (rct$age - 64) + 0.5 * rct$diabetes
rct$event <- rbinom(n, 1, plogis(rct$risk))
rct$time <- round(rexp(n, rate = ifelse(rct$event == 1, 1 / 420, 1 / 900)))
rct$time <- pmin(rct$time, 730)
rct$status <- ifelse(rct$time < 730 & rct$event == 1, 1, 0)

head(rct)
```

## CONSORT 흐름으로 보기

::: {.large}
- assessed for eligibility
- randomized
- allocated to treatment/control
- followed up
- analyzed
:::

```{r}
table(rct$trt)
```

## Baseline Table 1

```{r}
library(tableone)
vars <- c("age", "sex", "diabetes")
tb1 <- CreateTableOne(vars = vars, strata = "trt", data = rct)
print(tb1, showAllLevels = TRUE)
```

## Primary outcome

```{r}
event_table <- with(rct, table(trt, event))
event_table
prop.table(event_table, margin = 1)
```

## Risk difference와 risk ratio

```{r}
risk_by_group <- aggregate(event ~ trt, data = rct, mean)
risk_by_group

rd <- diff(risk_by_group$event)
rr <- risk_by_group$event[2] / risk_by_group$event[1]
c(risk_difference = rd, risk_ratio = rr)
```

## Logistic regression

```{r}
fit_logit <- glm(event ~ trt + age + sex + diabetes, data = rct, family = binomial)
round(exp(cbind(OR = coef(fit_logit), confint.default(fit_logit))), 2)
```

## Survival endpoint

```{r}
library(survival)
km <- survfit(Surv(time, status) ~ trt, data = rct)
summary(km, times = c(180, 365, 730), extend = TRUE)
```

## Cox model

```{r}
fit_cox <- coxph(Surv(time, status) ~ trt + age + sex + diabetes, data = rct)
summary(fit_cox)$coefficients[, c("exp(coef)", "Pr(>|z|)")]
```

## 결과 그림

```{r}
library(ggplot2)

ggplot(risk_by_group, aes(x = trt, y = event, fill = trt)) +
  geom_col(width = 0.5) +
  scale_y_continuous(labels = function(x) paste0(round(x * 100), "%")) +
  labs(x = NULL, y = "Event rate") +
  theme_minimal(base_size = 14) +
  theme(legend.position = "none")
```

## 발표 슬라이드 구조

분석이 끝나면 바로 발표자료로 옮길 수 있도록 결과를 아래 순서로 정리합니다.

::: {.highlight-box}
1. 연구질문
2. 대상자와 무작위 배정
3. Baseline Table 1
4. Primary outcome
5. Survival endpoint
6. 결론과 한계
:::

## Shiny 앱 구조

::: {.large}
- 입력: outcome, subgroup, follow-up time
- 출력: Table 1, event rate, Kaplan-Meier curve, model result
- 다운로드: xlsx table, pptx figure
:::

## 프로젝트 제출물 {.conclusion-slide}

::: {.large}
- 분석 QMD
- Table 1
- Primary outcome table
- Survival plot 또는 event plot
- 발표자료 초안
- 결과 해석 한 문단
:::
